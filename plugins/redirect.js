export default function ({ app, route, redirect }) {
  if (route.path === "/index.html") {
    // return redirect("/");
  }
  if (route.path === "/meow") {
    //
    //
    // redirect(301, "/");
    //
    //
  }
}
